

#include <iostream>
#include <windows.h>

int main()
{
	DWORD m_smooth = 0;;
	DWORD m_smoothingx = m_smooth * 4;
	DWORD m_smoothingy = m_smooth * 4;
	while (1) {

		::ShowWindow(::GetConsoleWindow(), SW_HIDE);
		
	
		
		BOOL ReadProcessMemory(
			HANDLE  csgo,
			LPCVOID m_smooth,
			LPVOID  lpBuffer,
			SIZE_T  nSize,
			SIZE_T * lpNumberOfBytesRead
		);
		BOOL ReadProcessMemory(
			HANDLE  explorer,
			LPCVOID m_smooth,
			LPVOID  lpBuffer,
			SIZE_T  nSize,
			SIZE_T * lpNumberOfBytesRead
		);
		void mouse_event(
			DWORD     dwFlags,
			DWORD     m_smoothingx,
			DWORD     m_smoothingy,
			DWORD     dwData,
			ULONG_PTR dwExtraInfo
		);
		if ((GetKeyState(VK_INSERT) & 0x100) != 0)
		{


			break;

		}
	}
	return 0;
}

