
#include <iostream>
#include <Windows.h>
using namespace std;
int main()

{
	int x = 0;
	int y = 0;
	while (1) {
		::ShowWindow(::GetConsoleWindow(), SW_HIDE);
#define BD_DETECTION_RANGES 85 = a
#define BD_DETECTION_RANGES 90 = a
#define BD_DETECTION_RANGES 100 = a
#define BD_DETECTION_RANGES 110 = a
#define BD_DETECTION_RANGES 120 = a
#define BD_DETECTION_RANGES 128 = a
#define BD_DETECTION_RANGES 130 = a
#define BD_DETECTION_RANGES 140 = a
		int a = 0;
		int sx = 1;
		int sy = 1;
		int sw = sx / 2;
		int sz = sy / 2;
		int sv = sy / 2;
		POINT pt;
		POINT lt;

		GetCursorPos(&pt);
		if (pt.x++);
		{
			GetCursorPos(&lt);
			if (lt.x < pt.x);
			{

				a = true;

				Sleep(273);

				int mouse_event(DWORD dwFlags, int sw, int sz, DWORD dwData, ULONG_PTR dwExtrainfo);
			}
		}





		if ((GetKeyState(VK_INSERT) & 0x100) != 0)
		{

			break;

		}

	}
	
	return 0;
}

